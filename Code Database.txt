CREATE TABLE clientes(

id INTEGER,
nome TEXT CONSTRAINT nn_ClientesNovos_nome NOT NULL,
endereco TEXT,
num_end TEXT,
bairro_end TEXT,
cidade_end TEXT,
cep_end TEXT,
telefone TEXT,
telefax TEXT,
celular TEXT,
cnpj_cpf TEXT,
insc_rg TEXT,
end_entrega TEXT,
num_end_entrega TEXT,
bairro_end_entrega TEXT,
cid_end_entrega TEXT,
cep_end_entrega TEXT,
local_trab TEXT,
end_local_trab TEXT,
telefone_trab TEXT,
filiacao_pai TEXT,
filiacao_mae TEXT,
data_cad TIMESTAMP,
data_nasc TIMESTAMP,
limite_cred TEXT,
contato TEXT,
tipo_contato TEXT,
ativo TEXT,
complemento TEXT,
compl_ent TEXT,
uf_end TEXT,
uf_end_ent TEXT,
site TEXT,
email TEXT,
codcidade TEXT,
sincronizado TEXT,
CONSTRAINT pk_clientes_id PRIMARY KEY(id)

);



-----------------------------------------------------

CREATE TABLE pedidos(
	
	id INTEGER CONSTRAINT nn_pedidos_id NOT NULL,
	pedido_num INTEGER CONSTRAINT nn_pedidos_pedido_num NOT NULL,
	idcliente INTEGER CONSTRAINT nn_pedidos_idcliente NOT NULL,
	id_vendedor INTEGER CONSTRAINT nn_pedidos_id_vendedor NOT NULL,
	data_pedido TIMESTAMP CONSTRAINT nn_pedidos_data_pedido NOT NULL,
	hora_pedido TIME CONSTRAINT nn_pedidos_hora_pedido NOT NULL,
	data_entrega TIMESTAMP,
	hora_entrega TIMESTAMP,
	id_classificao INTEGER CONSTRAINT nn_pedidos_id_classificacao NOT NULL,
	confirmada CHAR(1) CONSTRAINT nn_pedidos_confirmada NOT NULL,
	entregue CHAR(1) CONSTRAINT nn_pedidos_entregue NOT NULL,
	valor NUMERIC(7,2),
	desconto NUMERIC(6,2),
	acrescimo NUMERIC(6,2),
	outras_despesas NUMERIC(6,2),
	valor_total NUMERIC(6,2),
	cancelado CHAR(1) CONSTRAINT nn_pedidos_cancelado NOT NULL,
	id_formapag INTEGER CONSTRAINT nn_pedidos_id_formapag NOT NULL,
	uf_end_entrega CHAR(2),
	tipodoc INTEGER,
	tipo_pedido INTEGER,
	valor_real NUMERIC(6,2),
	margem_venda NUMERIC(6,2),
	valor_comissao NUMERIC(6,2),
	total_geral NUMERIC(6,2),
	coo INTEGER,
	ccf INTEGER,
	gnf INTEGER,
	dav CHAR(1),
	valor_troco NUMERIC(6,2),
	caixa INTEGER,
	idempresa INTEGER,
	dav_impresso INTEGER,
	natop TEXT,
	aliq_icms INTEGER,
	base_icms INTEGER,
	valor_icms INTEGER,
	aliq_icms_st INTEGER,
	base_icms_st INTEGER,
	valor_icms_st INTEGER,
	valor_pis INTEGER,
	valor_cofins INTEGER,
	valor_ipi INTEGER,
	md5registro TEXT,
	numpv INTEGER,
	cer INTEGER,
	id_mesa INTEGER,
	abastecimento CHAR(1),
	status_abastec INTEGER,
	numserieecf TEXT,
	placa_veic TEXT,
	odometro TEXT,
	chave_nfe TEXT,
	status_nfe TEXT,
	protocolo_nfe TEXT,
	recibo_nfe TEXT,
	devolucao CHAR(1),
	numero_doc INTEGER,
	sincronizado CHAR(1),
	idusuariocanc INTEGER,
	valor_carga_trib TIMESTAMP,
	numdav INTEGER,
	tipo_frete INTEGER,
	valor_frete NUMERIC(6,2),
	valor_seguro NUMERIC(6,2),
	valor_outros NUMERIC(6,2),
	base_iss NUMERIC (6,2),
	valor_iss INTEGER,
	base_pis NUMERIC (6,2),
	base_cofins NUMERIC (6,2),
	base_ipi NUMERIC (6,2),
	idplanocontas INTEGER,
	idcentrocusto INTEGER,
	nfe_tipoemiss INTEGER,
	nautoriza_vidalink INTEGER,
	num_lote INTEGER,
	arquivo_xml TEXT,
	val_carga_fed INTEGER,
	val_carga_EST INTEGER,
	val_carga_mun INTEGER,
	notamanual CHAR(1),
	coo_conf_mesa INTEGER,
	modelo INTEGER,
	data_autoriza TEXT,
	hora_autoriza TEXT,
	obsnfe_1 TEXT,
	obsnfe_2 TEXT,
	obsnfe_3 TEXT,
	obsnfe_4 TEXT,
	codigosefaz TEXT,
	numerosessao INTEGER,
	desconto_prod NUMERIC(6,2),
	transp_modfrete TEXT,
	transp_cnpjcpf TEXT,
	transp_xnome TEXT,
	transp_ie TEXT,
	transp_endereco TEXT,
	transp_municipio TEXT,
	transp_uf TEXT,
	transp_valserv INTEGER,
	transp_valbcret INTEGER,
	transp_percicmsret INTEGER,
	transp_valicmsret INTEGER,
	transp_cfop INTEGER,
	transp_codmunicp INTEGER,
	transp_placa INTEGER,
	transp_RNTC INTEGER,
	transp_uf_placa INTEGER,
	valor_fcp INTEGER,
	valor_icms_interno INTEGER,
	valor_icms_dest INTEGER,
	controle_entrega CHAR(1),
	CONSTRAINT pk_pedidos_id PRIMARY KEY(id),
	CONSTRAINT fk_clientes_id FOREIGN KEY(idcliente) REFERENCES clientes(id)
);

-------------------------------------------------------
CREATE TABLE pedidos_itens(
	
	id INTEGER CONSTRAINT nn_pedidos_itens_id NOT NULL,
	idpedido INTEGER CONSTRAINT nn_pedidos_itens_idpedido NOT NULL,
	idproduto INTEGER CONSTRAINT nn_pedidos_itens_idproduto NOT NULL,
	descricao TEXT,
	quantidade NUMERIC(6,2),
	valor NUMERIC(6,2),
	desconto NUMERIC(6,2),
	acrescimo NUMERIC(6,2),
	valor_total NUMERIC(6,2),
	margem NUMERIC(6,2),
	valor_real NUMERIC (6,2),
	tipoprod INTEGER,
	cancelado CHAR(1),
	num_item INTEGER,
	ean13 BIGINT,
	cst INTEGER,
	cfop INTEGER,
	aliq_icms INTEGER,
	base_icms INTEGER,
	valor_icms INTEGER,
	reducao_base INTEGER,
	aliq_icms_st INTEGER,
	base_icms_st INTEGER,
	valor_icms_st INTEGER,
	valor_pis INTEGER,
	valor_cofins INTEGER,
	valor_ipi INTEGER,
	md5registro TEXT,
	id_bico INTEGER,
	id_tanque INTEGER,
	id_bomba INTEGER,
	ei INTEGER,
	ef INTEGER,
	mva NUMERIC(4,2), 
	id_vendedor INTEGER,
	coo INTEGER,
	ccf INTEGER,
	gnf INTEGER,
	numserieecf INTEGER,
	id_abastecimento INTEGER,
	aliq_pis INTEGER,
	aliq_cofins INTEGER,
	base_pis NUMERIC(6,2),
	base_cofins NUMERIC(6,2),
	aliq_ipi INTEGER,
	base_ipi NUMERIC(6,2),
	cst_ipi INTEGER,
	cst_pis INTEGER,
	cst_cofins INTEGER,
	aliq_iss INTEGER,
	base_iss INTEGER,
	valor_iss INTEGER,
	unidade INTEGER,
	valor_reenbolso NUMERIC(6,2),
	valor_subsidio NUMERIC(6,2),
	id_bico_perda INTEGER,
	id_cor INTEGER,
	id_tamanho INTEGER,
	csosn INTEGER,
	desconto_total NUMERIC(6,2),
	perc_fcp INTEGER,
	aliq_interno INTEGER,
	perc_difal INTEGER,
	perc_part_difal INTEGER,
	valor_fcp NUMERIC(6,2),
	valor_icms_interno NUMERIC(6,2),
	valor_icms_dest NUMERIC(6,2),
	CONSTRAINT pk_pedidos_itens_id PRIMARY KEY(id),
	CONSTRAINT fk_pedidos_itens_id FOREIGN KEY(idpedido) REFERENCES pedidos(id)

);
